export function putNumbersInArray(a: number, b: number, c: number): number[] {
    // Put a, b, and c in an array. a first, then b, then c
    return []
}

export function putNumbersInArraySorted(a: number, b: number, c: number): number[] {
    // Put a, b, and c in an array, sorted from least to greatest.
    return [];
}

export function changeElementToHello(array: string[], index: number): string[] {
    // Change the element at the given index to 'hello'. If the index is
    // out of bounds (negative or above the length of the array), do nothing.

    return [];
}

export function changeElement(array: string[], index: number, newValue: string): string[] {
    // Same thing, but instead of changing the value to 'hello', change it
    // to the given value.

    return [];
}

export function findHello(array: string[]): number {
    // Return the index of 'hello' in the array, or return -1 if it does not exist
    return -1;
}

export function nestedArrayGet(array: number[][]): number {
    // array is a 2D (nested) array, or an array in an array. 
    // Return the first element in the first array in array.
    return 0;
}